#Tuple does not have any operation like append, extend, remove, pop etc
#Tuple is immutable
#Tuple is faster than list

#Creating a tuple
tup = (1,2,3,4,5)
# print(tup.append(7))
# convert tuple into list to perform any operation like append, extend, remove, pop etc
tup=list(tup)
print(type(tup))
